import { Strategy, StrategyResult } from "@/types";
export declare function backtest(
  strategy: Strategy,
  startDate: Date,
  endDate: Date,
): Promise<StrategyResult>;
//# sourceMappingURL=backtesting.d.ts.map
